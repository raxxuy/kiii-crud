apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: kiii-crud
spec:
  selector:
    app: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  clusterIP: None # headless service required for StatefulSet
